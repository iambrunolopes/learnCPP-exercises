#include <iostream>

int oneReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-128);
    else
        newInput=input;
return newInput;
}

int twoReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-64);
    else
        newInput=input;
return newInput;
}

int threeReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-32);
    else
        newInput=input;
return newInput;
}

int fourReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-16);
    else
        newInput=input;
return newInput;
}

int fiveReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-8);
    else
        newInput=input;
return newInput;
}

int sixReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-4);
    else
        newInput=input;
return newInput;
}

int sevenReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-2);
    else
        newInput=input;
return newInput;
}

int eightReturned(int x, int input)
{
int newInput{};
    if (x==1)
        newInput=(input-1);
    else
        newInput=input;
return newInput;
}
